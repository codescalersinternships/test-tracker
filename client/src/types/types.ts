export type ProfileSettings = {
    firstName: string,
    lastName: string,
    phoneNumber: string,
}

export enum AlertType {
    Success = 'success',
    error = 'error',
    warning = 'warning',
}
